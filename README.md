
  # Casa Descansa (web app)

  This is a code bundle for Casa Descansa (web app). The original project is available at https://chase-hall-73928566.figma.site/ 

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
